package edu.syr.bookrepository;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class BookRepositoryApplication {
    // This class serves as the entry point for the Book Repository Spring Boot application.

    public static void main(String[] args) {
        // The main method that starts the Spring Boot application.
        SpringApplication.run(BookRepositoryApplication.class, args);
    }
}
